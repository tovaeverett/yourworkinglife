import java.lang.Math.pow
import java.nio.file.*

val power = 10.0
Math.pow(2.0, power)

fun isEven(d: Double) = d.toInt() % 2 == 0
isEven(res3)

Paths.get("/Users/me.user")


Files.walk(res6, 1).forEach { println(it) }
